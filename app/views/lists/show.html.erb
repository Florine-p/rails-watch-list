<h1>Hello</h1>

<ol>

<% @list.movies.each do |movie| %>
<li>
  <%= movie.title %>
  <%= movie.overview %>
  <%= image_tag movie.poster_url, alt:"image" %>
  <%= movie.rating %>
  </li>
<% end %>
</ol>