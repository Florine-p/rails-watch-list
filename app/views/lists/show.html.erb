<h1> <%= @list.name %> </h1>
<ol>
<% @list.movies.each do |movie| %>
<li>
  <%= movie.title %>
  <%= movie.overview %>
  <%= image_tag movie.poster_url, alt:"image", class: "movie-img" %>
  <%= movie.rating %>
  </li>
<% end %>
</ol>
<%= link_to "Add movies", new_list_bookmark_path(@list)  %>